---
'@galosandoval/shopfloor': minor
---

Require Node 24. `engines` is now `>=24`, the build targets `node24`, the
release workflow and the scaffolded consumer workflow install node 24, and
`@types/node` moves to `^24`.
